# V0.2 任务清单

目标：Three.js 渲染器把 hospital_ward_min 场景 + Stretch 机器人真正显示出来。

## 进行中

- [ ] **Phase 1a — 渲染器视觉确认**（阻塞）
  - 代码已完成：`src/sim/mujocoRenderer.ts`（每帧 `mj_step` 后镜像 `data.geom_xpos / geom_xmat` 到 `THREE.Mesh.matrix`）
  - 10 个 MJCF camera 可切换（targetbody 模式 `cam_mode=3`，fixed 模式走 `cam_xmat` 前向轴）
  - 所有 6 种 primitive geom（plane / sphere / capsule / ellipsoid / cylinder / box）原生渲染
  - mesh-type geom 暂用 3cm 彩色小球占位（Phase 1b 替换）
  - **待用户操作**：`npm run dev` → http://localhost:5173 → 点 "Load hospital_ward_min.xml (Phase 0F)" → 检查：
    - [ ] 地板 / 墙 / 床（x≈3.8）/ 患者身体（capsule 组）可见
    - [ ] 床头柜上蓝色杯子，椅子（y≈1.5），墙上电视
    - [ ] 机器人在 origin 附近呈现彩色小球簇（占位正常）
    - [ ] 相机选择器切换 10 个视角不报错

## 待开始

- [ ] **Phase 1b — OBJ/STL 加载器**
  - 用 Three `OBJLoader` / `STLLoader` 替换占位球
  - 通过 `model.geom_dataid[i]` 查 `name_meshadr[]` 得文件名
  - 材质：`geom_matid` → `mat_texid` → `name_texadr[]` → PNG 纹理（13 张）
  - 按 mesh filename 缓存，避免重复 fetch
  - 预算：总资源 ~96MB 不变，内存按需释放

- [ ] **Phase 2 — Web Worker 物理线程**
  - `mj_step` 移出主线程
  - SharedArrayBuffer 状态总线（需要 COOP/COEP，vite.config.ts dev 已配，GH Pages 需验证）
  - 回退：postMessage structured clone

- [ ] **Phase 3 — 行为树引擎 `src/bt/`**
  - `status.ts`（SUCCESS/FAILURE/RUNNING）
  - `node.ts`（Behaviour 基类）
  - `composites.ts`（Sequence/Selector，含 memory）
  - `tree.ts`（BehaviourTree + tick loop）
  - 语义只覆盖 `demo/core/anima/l2_planner.py` 用到的 py_trees 子集
  - vitest 单测：序列部分成功 / memory 开关 / ticks_to_success mock

- [ ] **Phase 4 — Anima L0–L5 端口**
  - 端口源：`human-brain-interface-demo/demo/core/anima/`
  - 目标文件：`src/anima/{l0_neural,l1_parser,l2_planner,l3_skill,l4_adapter,l5_assessment,taskspec,gates}.ts`
  - L4 `WasmStretchAdapter` 包装 `mujoco.mj_data.ctrl` 写入
  - L5 PEA 日志走 IndexedDB（替代 Python 的文件系统）
  - TaskSpec 用 zod 替代 pydantic

- [ ] **Phase 5 — 10 个技能**
  - 1:1 翻译 `demo/core/anima/l3_skill.py`（~520 行）
  - 每个技能一个 TS 文件 + 一个 vitest
  - `src/skills/` + `src/skills/registry.ts`

- [ ] **Phase 6 — L1 关键词映射**
  - `src/llm/keyword_mapper.ts`：35 intent × 中英双语表 → TaskSpec
  - 无 fetch，无 LLM
  - 插入 `l1_parser.ts` 作为唯一实现，保留 `drift_score` 行为兼容 L5
  - UI 必须明示"演示用映射器，完整 LLM-Parser 见 demo/core"

- [ ] **Phase 7 — UI 外壳**
  - 布局：L0-L5 stack（左）+ 3D viewport（中）+ BT viz（右）+ 五因素面板 + 事件流（下）
  - 6 个 ADL 意图按钮 + 自由文本
  - 参考 `demo/web/` 布局但独立重建，不共享代码

- [ ] **Phase 8 — GitHub Pages 部署**
  - `.github/workflows/deploy.yml`：build with `VITE_BASE=/mujoco-browser/` → gh-pages 分支
  - 确认 COOP/COEP 头（若 Phase 2 用 SAB）
  - 最终链接：`https://jeffliulab.github.io/mujoco-browser/`

## 已完成（V0.1）

- [x] Vite + React + TS 脚手架
- [x] mujoco-js 0.0.7 接入 + headless smoke（simple / humanoid）
- [x] Three.js 占位 canvas（旋转立方体）
- [x] stretch_mujoco 资源打包（bundle-assets.ts → 96.6 MB / 147 files）
- [x] manifest.json + 浏览器 VFS 种子路径
- [x] stretch_mj_3.3.0.xml（robot only）headless smoke 通过
- [x] hospital_ward_min.xml 降级场景（所有 madduck mesh → primitive 替代）
- [x] 4 场景 headless smoke 全过
- [x] dev server 端到端 HTTP 资源验证

## 阻塞

暂无。等 Phase 1a 视觉确认就能继续。
